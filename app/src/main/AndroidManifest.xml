<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kyle.uwitimemanagemeent">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RegisterActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".WelcomeActivity" />
        <activity
            android:name=".DefaultMonthlyCalendarActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AddTask" />
        <activity android:name=".AddTaskDetails" />
        <activity android:name=".DatabaseHelper" />
        <activity android:name=".AddSchoolTask" />
        <activity android:name=".SchoolDataBaseHelper" />
        <activity android:name=".CardActivity" />
        <activity android:name=".ShowAndUpdateGenTask" />
        <activity android:name=".SchoolCard" />
        <activity android:name=".SchoolCardActivity" />
        <activity android:name=".ShowAndUpdateSchTask" />
        <activity android:name=".ElearningWebView"></activity>
    </application>

</manifest>